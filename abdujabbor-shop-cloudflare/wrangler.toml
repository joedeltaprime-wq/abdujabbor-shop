name = "abdujabbor-shop"
pages_build_output_dir = "."
compatibility_date = "2026-09-01"

# Cloudflare Dashboard -> Workers & Pages -> your project -> Settings -> Bindings
# Add a KV namespace binding named: STORE
# Add secrets:
#   ADMIN_PASSWORD = your strong admin password
#   SESSION_SECRET = a long random secret
